# Cyber skills index

Generated by [`scripts/knowledge/generate-cyber-skills-index.py`](../../scripts/knowledge/generate-cyber-skills-index.py) from [`.external/Anthropic-Cybersecurity-Skills-main/`](../../.external/Anthropic-Cybersecurity-Skills-main/).

## Regenerate

```bash
make skills-index
```

## `cyber-skills.json` fields (per skill)

| Field | Description |
|-------|-------------|
| `id` | Directory slug (stable key) |
| `name` | Frontmatter `name` |
| `domain` / `subdomain` | Taxonomy from upstream |
| `description` | Short summary from frontmatter |
| `tags` | String list |
| `nist_csf` | NIST CSF control ids when present |
| `attack_ids` | MITRE ATT&CK technique ids parsed from file |
| `external_path` | Repo-relative path to `SKILL.md` |
| `body_chars` | Markdown body size (metadata only) |

Top-level: `skill_count`, `unique_attack_ids`, `subdomain_counts`, `generated_at`.

## Consumers

- `knowledge/serve` playbook usecase + MCP `playbook_*`
- `knowledge/ingest/cmd/playbook_seed` (Neo4j `CyberSkill` + `HAS_PLAYBOOK`)
