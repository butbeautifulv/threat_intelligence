name: platform

on:
  workflow_dispatch:
    inputs:
      run_full_loop:
        description: Run P4b full loop smoke (heavy Docker)
        type: boolean
        default: false
  push:
    branches: [main, master]
    paths:
      - 'pkg/harvest/**'
      - 'pkg/commit/**'
      - 'pkg/natsjet/**'
      - 'pkg/engage/**'
      - 'pipeline/connector/**'
      - 'pipeline/ned/**'
      - 'pipeline/engage-events/**'
      - 'graph/ingest/**'
      - 'graph/connector/**'
      - 'engage/serve/internal/client/veilgraph/**'
      - 'engage/serve/internal/usecase/intelligence/**'
      - 'scripts/test/smoke-platform*.sh'
      - 'docs/platform-closed-loop-pilot.md'
      - 'docs/platform-full-loop-smoke.md'
      - 'deploy/terraform/**'
      - 'Makefile'
      - 'versions.env'
      - '.github/workflows/platform.yml'
  pull_request:
    paths:
      - 'pkg/harvest/**'
      - 'pkg/commit/**'
      - 'pkg/natsjet/**'
      - 'pkg/engage/**'
      - 'pipeline/connector/**'
      - 'pipeline/ned/**'
      - 'pipeline/engage-events/**'
      - 'graph/ingest/**'
      - 'graph/connector/**'
      - 'engage/serve/internal/client/veilgraph/**'
      - 'engage/serve/internal/usecase/intelligence/**'
      - 'scripts/test/smoke-platform*.sh'
      - 'docs/platform-closed-loop-pilot.md'
      - 'docs/platform-full-loop-smoke.md'
      - 'deploy/terraform/**'
      - 'Makefile'
      - 'versions.env'
      - '.github/workflows/platform.yml'

jobs:
  unit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Platform bus unit tests (P0)
        run: make test-platform-p0

  closed-loop:
    runs-on: ubuntu-latest
    needs: unit
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - name: Docker version
        run: docker compose version

      - name: Closed-loop pilot smoke (P3)
        env:
          SMOKE_VEIL_STACK_BUILD: '1'
        run: make test-platform-closed-loop

  full-loop:
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' && inputs.run_full_loop == true
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      - name: Docker version
        run: docker compose version

      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Platform P0 unit tests
        run: make test-platform-p0

      - name: Full loop smoke (P4b)
        env:
          SMOKE_VEIL_STACK_BUILD: '1'
          SMOKE_SCRAPE_WAIT_SEC: '900'
        run: make test-platform-full-loop
