# Populate from Terraform outputs (P5b): ansible_host, ansible_user, groups.
all:
  children:
    veil_data:
      hosts:
        veil-data-01:
          ansible_host: 127.0.0.1  # placeholder — replace after TF apply
    veil_scrape:
      hosts:
        veil-scrape-01:
          ansible_host: 127.0.0.1
