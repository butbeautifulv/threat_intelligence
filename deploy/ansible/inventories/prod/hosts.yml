all:
  children:
    veil_data:
      hosts:
        veil-data-01:
          ansible_host: 127.0.0.1  # placeholder
    veil_scrape:
      hosts:
        veil-scrape-01:
          ansible_host: 127.0.0.1
