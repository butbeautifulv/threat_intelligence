{{- if .Values.pipeline.worker.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "veil.fullname" . }}-pipeline-worker
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "veil.fullname" . }}-pipeline-worker
  minReplicas: {{ .Values.pipeline.worker.hpa.minReplicas }}
  maxReplicas: {{ .Values.pipeline.worker.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
{{- end }}
