limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=mcp:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=graph_api:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=graph_mcp:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=engage_api:10m rate=20r/s;
limit_req_zone $binary_remote_addr zone=engage_mcp:10m rate=10r/s;

client_max_body_size 8m;
