{
  "mcpServers": {
    "veil-graph-http": {
      "url": "http://localhost:8091/mcp",
      "headers": {
        "Authorization": "Bearer REPLACE_WITH_KEYCLOAK_ACCESS_TOKEN"
      },
      "description": "Veil graph MCP over Streamable HTTP (MCP_HTTP_ENABLED=1 on server)",
      "timeout": 300
    }
  }
}
